# TypeScript hello world

Task queue: `hello-world-typescript`.

```bash
# from repo root, with Temporal server running
cd typescript
npm run start.watch
```

In another terminal:

```bash
cd typescript
npm run workflow
```
